schema_version: 1

context:
  name: factur-x
  version: "6.2"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/${{ name | replace('-', '_') }}-${{ version }}.tar.gz
  sha256: e3b8add147be66f05e4dffae887f536499c39728d7602e43d26efc0783b39423

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - setuptools
    - hatchling
    - hatch-requirements-txt
  run:
    - python >=${{ python_min }}
    - pypdf >=5.3.0
    - lxml
    - python-stdnum
    - requests
tests:
  - python:
      imports:
        - facturx
      pip_check: true
      python_version: ${{ python_min }}

about:
  summary: 'Factur-X and Order-X: electronic invoicing and ordering standards'
  license: BSD-3-Clause
  license_file: LICENSE
  homepage: https://github.com/akretion/factur-x

extra:
  recipe-maintainers:
    - jan-janssen
